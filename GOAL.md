This skill needs to be improved a lot. the setup it creates is mediocre. The questions it asks are mediocre. 
I ran it on the /Users/ravisojitra/Documents/projects/riffads project, it's going way too niche.
It's not even creating any skill. I don't want to have cap limits on anything, it said that i have the cap limit, it should never say it and deliver the full agentic setup.
This should not consider the user's global level skills, we are here to create the personalized setup of claude system that levels up user's development and personalized agents and skills.

now it asked me below questions:
1) When I'm done, open a pull request, or leave the branch for you?
2) Package this as a Claude Code plugin (.claude-plugin/plugin.json) so you can install it in other repos?

and these questions must never be asked it's pointless. 1-> it should never open PR. 2) no, since this is going to be personalized to the selected user's repo only, it won't be installed anywhere.

3) You already have a mature setup — 13 of the 61 skills and agents here are your own (the other 48 are third-party, installed rather than written here); 18 of them are symlinked in from a shared store. I'll run audit-only: additions and fixes, nothing restructured. Good?
Why: provenance.maturity_basis counts 13 as yours, and 5 or more triggers audit-only.
3-> this should only consider project's setup and not global skills. we are here to create personalized agentic claude setup of the user's selected codebase.

Now, this is how I started working on the client's project so that it can give you idea on how to think and how to create the setup:
1) I understood their codebase and messages sent by them to claude or codex, saw and understood where they struggled or what most they were frustrated with.
2) I then mapped out all the components they are using that can be converted into 
    - agents such as db-manager, pr reviewer, security auditor, designer, product analyst etc.
    - hooks for auto lint, blocking destructive commands, enforcing chosen package manager, env leak detector like blocking ai to read env files etc.
    - rules for all the components like database, linear workflow, memory recording, testing, ui, branching, code style, and since they had monorepo containing api and web, i had also created rules for web and api. So this is something you will need to consider depending on the user's codebase). I understood their git branching, code commit style, project management since they had linear so had created linear-workflow.md and like that. 
    - then I went to understand their workflows and had started creating skills.
    - I understood that they were using linear, so i had created the create-linear-issue skill that had reference files like personalized project conventions, output format, guardrails, context gathering and all which used to explore the codebase to find out the best practices and created skill for it personalized to the project.
    - then i knew that they were using posthog so i had created add product analytics(to add the certain events that follows products KPIs and all), ask-product(that does readonly posthog queries to answer certain questions kind of things), posthog-dashboard(that could create a new dashboard widget from the events added in the codebase to see) then I had created pr-reviewer skill that used to review anyone's PRs to find out any rules broken or issues, or bugs or improvements etc. then i had created /qa skill which was built on top of agent-browser, this was personalized skill not just random instructions like it could get url as an input to test against and if login is required it needs to stop to ask user, ask for goal kind of skill and then qa against it and then qa output format etc, then customized design skill that was personalized to user's codebase design guidelines, since they were using neon, i had created a skill that used to query read only queries on neon using sub agent db-inspector. so it had only read only allowed tools. so you need to understand all these tactics in this agentify skill so when it creates skills and all for users, it generates personalized setup in the user's codebase.
    - Then I had created a really detailed setup-manager skill, this skill kind of the one above all which means since every skills and rules and hooks and agents were created automatically with our agentify skill, user certainly will need to update the skills or the configurations created by our agentify skill, so this setup-manager skill will have all the latest knowledge on how all those coding agents config works and can create,extend,modify their setup. so user can ask to create/update any skill,rule,config,agents etc.
3)  then all those hooks that i had created were added in the settings.json  with pretooluse using block destructive command, env leak, command manager, etc then had created permissions allow and deny list in the .claude/settings.json.
4) then i had enabled mcp servers for all the third party integrations that we had created and used skills,rules, agents for like posthog, linear, neon, etc. This will be personalized based on the user's codebase.
5) Then i asked them whether they use any engineering system like every's compound engineering or obra's superpowers plugin? they will say they were using compound engineering or want to use compound engineering. So, i installed it for them from the marketplace.
6) now after doing all of this, one last thing remains which is stitching everything together. we will need to update claude/agents.md depending on for which we are creating. and then all the config that we added needs to go in the CLAUDE/AGENTs.md. so for example, i added compound engineering workflow guide, then i had added branch&PR rules from their guide, then i added subagents table containing name, 2 liner desc, trigger, when to invoke,then skills table on skill name, description, how to invoke, then had added rules table path scoped guardrailes which contained where all the rules live, rule name, file, scope, what it governs. Be as direct and creative as possible, interaction rules. One thing we will need to mention in our skill is that they never remove or update CLAUDE.MD/AGENTS.md user's instructions and make most of the additive changes. and explain them. 

When I mentioned the personalized to user's codebase, all it mean is you will need to write or add instructions around user's codebase and how user works, how their codebase is, etc. So, user really feel that this agentify skill is not generalized and same for all but it's fucking so much personalized.